# Implementation notes

## Current scaffold status

This app scaffold is intentionally non-buildable in the current environment because Java and the Android toolchain are not installed.

## Recommended next step in Android Studio

1. Create a Kotlin + Compose app in this folder.
2. Reuse the package name `se.neo1bit.banprofil`.
3. Keep the data/domain/ui separation from this scaffold.
4. Start by rendering the polished mockups with static data.
5. Add JSON parsing for `banprofil-route.schema.json` compatible route packages.
6. Integrate route-constrained positioning after static UI is stable.
